/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow local hostnames used during development (localhost / 127.0.0.1).
  allowedDevOrigins: ["localhost", "127.0.0.1"],
};

export default nextConfig;
